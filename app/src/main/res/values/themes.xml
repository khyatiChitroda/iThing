<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.IThingMobile" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Pre-Android 12 splash theme (v31+ overrides this via Theme.SplashScreen). -->
    <style name="Theme.IThingMobile.Splash" parent="Theme.IThingMobile">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>
